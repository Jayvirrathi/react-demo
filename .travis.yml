language: node_js
node_js: 
-14.0.0
cache:npm

install:
    -yarn install

script:
    -yarn test
    -yarn run build

deploy:
    provider:pages
    skip-cleanup: true
    local_dir:build
    github-token :$GITHUB_TOKEN
    on: 
        brach: master